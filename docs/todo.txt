Trainable: decoder LoRA (self- and cross-attn QKV/O; optional MLP proj). Keep encoder frozen.
LR: 3e-4 (range 1e-4–5e-4), AdamW, weight decay 0.01, grad clip 1.0.
LoRA: rank 8–16 (start 16), alpha 16–32, dropout 0.05.

Trainable: decoder LoRA (self- and cross-attn QKV/O; optional MLP proj)
Only adapt small LoRA adapters on the decoder’s attention projections:
Self-attention and encoder-decoder attention: q_proj, k_proj, v_proj, out_proj.
Optionally also add LoRA on the MLP projections (fc1, fc2) for a bit more capacity.
Keep the encoder frozen so it doesn’t update (saves compute and avoids overfitting).
LR, optimizer, regularization
learning_rate ≈ 3e-4 (works well with LoRA since few params are trainable). Safe range: 1e-4–5e-4.
AdamW optimizer, weight_decay=0.01, gradient clipping (max_grad_norm)=1.0.
LoRA hyperparameters
rank r: 8–16 (start with 16). Controls adapter bottleneck size.
alpha: 16–32. Scaling factor for the LoRA updates (effective scale ~ alpha/r).
dropout: 0.05. Regularizes the LoRA path.

Add LoRA to the decoder attention projections (and optionally MLP).
Freeze the encoder after injecting LoRA so that any LoRA params in the encoder (if injected) remain frozen.
Ensure LR/optimizer/clip/weight decay are set.

